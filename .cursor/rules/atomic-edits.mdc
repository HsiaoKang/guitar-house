---
description: 编辑代码时保证文件级原子性，避免 dev server HMR 编译到中间态报假错误
alwaysApply: true
---

# 文件级原子编辑

本项目开发时通常有 `pnpm dev`（Vite HMR + Tauri）在后台运行，每次写盘都会立即触发编译。

- 同一文件内彼此依赖的改动（如 JSX 开/闭标签、函数声明与调用、import 与使用），必须合并为一次编辑写盘，不允许拆成多次编辑留下语法不完整的中间态
- 无法用一次替换覆盖时，宁可替换一个包含全部改动点的大块（连同中间未变的代码），也不要分次写入
- 跨文件的依赖改动按依赖方向排序：先写被依赖方（导出、类型定义），再写依赖方（导入、调用）
- 若编辑失误已触发 HMR 报错，修复后在回复中主动说明该报错为编辑中间态误报
